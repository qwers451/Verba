# RAG Quality Test v2 (cosine similarity): Matan Textbook

**Material ID:** `10d95aea-ab1e-473f-8120-73901fdd37a4`  
**Total chunks indexed:** 339

---

## Query: "производная функции определение правило"

### #1 — score: 0.7530 🟢 Отлично

```
# 9.1. Определение и геометрический смысл производной функции в точке

Определение 9.1. Производной функции y = f(x) в точке x называется число

f (x) = lim f(x + ∆x) − f(x),

∆x→0 ∆x

если такой предел существует.

Задача о проведении касательной к графику функции y = f(x) в точке x тоже приводит к необходимости решить подобного рода предельный переход.

Рассмотрим, например, функцию y = ax² и ее график (рис. 9.1). Проведем касательную к этой кривой в точке M(x, ax²). Касательной к кривой в точ
```

### #2 — score: 0.6998 🟡 Приемлемо

```
# 15. Дифференцируемость функции многих переменных

# 15.1. Частные производные

Определение 15.1. Пусть функция f(x, y) определена в некоторой окрестности точки (x₀, y₀). Если существуют конечные пределы

lim f(x₀ + ∆x, y₀) − f(x₀, y₀)

∆x→0                                 ∆x

и

lim f(x₀, y₀ + ∆y) − f(x₀, y₀)

∆y→0                                 ∆y

то их называют частными производными функции f в точке (x₀, y₀) по переменным x и y соответственно и обозначают

∂f (x₀, y₀ ) или f' (x₀ , y₀ )  
```

### #3 — score: 0.6774 🟡 Приемлемо

```
Аналогично определяются дифференциалы более высоких порядков:

dⁿy = δ (d(ⁿ−1)y)|.

| x = x0

δx = dx

# 9.6. Производная функции, заданной параметрически

Пусть функции

{ x = x(t), (9.5)

y = y(t)

определены, непрерывны на некотором промежутке (α, β), а функция x(t) строго монотонна на нем. Тогда для функции x(t) существует обратная функция t = t(x), определенная на некотором промежутке (a, b) и, следовательно, имеет смысл сложная функция f(x) = y(t(x)). Эта функция y = f(x) называется функци
```

---

## Query: "интеграл Ньютона-Лейбница формула"

### #1 — score: 0.6755 🟡 Приемлемо

```
# 11.5. Интеграл с переменным верхним пределом. Формула Ньютона–Лейбница

Пусть функция f интегрируема на [a, b]. Функция

F(x) = ∫ax f(t)dt, x ∈ [a, b],

называется интегралом с переменным верхним пределом интегрирования.

# Теорема 11.8.

Пусть функция f интегрируема на отрезке [a, b]. Тогда интеграл с переменным верхним пределом интегрирования непрерывен на отрезке [a, b].

# Доказательство.

Из интегрируемости функции f следует ее ограниченность, т. е. существует M > 0 такое, что |f(t)| ≤ M 
```

### #2 — score: 0.6682 🟡 Приемлемо

```
# П р и м е р 23.2.

Вычислить криволинейный интеграл

∫ (ₓ₂ + y₂) dx + 2xy dy

AB

вдоль дуги кубической параболы y = x³ от точки A(1; 1) до точки B(2; 8).

# Р е ш е н и е.

Применяя формулу (23.4), получим

∫ (ₓ₂ + y₂) dx + 2xy dy =

AB

2

= ∫ ((x² + (x³)²) + 2x · x³ · 3x²) dx =

1

2

= ∫(x² + 7x⁶) dx = 129  1.

3

1

# Определение 23.2.

Если криволинейный интеграл

∫ Ф dr от вектор-функции

Ф = P(x, y, z )i + Q(x, y, z )j + R(x, y, z )k

берется по замкнутому контуру L, то он называется к
```

### #3 — score: 0.6627 🟡 Приемлемо

```
# Д о к а з а т е л ь с т в о.

По формуле Ньютона–Лейбница

b
∫ [u(ₓ)v(ₓ)]'dx = u(ₓ)v(ₓ)|ᵇ.
|
a

Учитывая, что

[u(x)v(x)]' = u' (x)v(x) + u(x)v' (x),

получаем

b
∫ [u(ₓ)v(ₓ)]'dx = ∫     u' (x)v(x)dx + ∫  u(x)v' (x)dx.
a

Отсюда следует требуемое равенство.

Последнюю формулу удобно записать в виде

b
∫ u(ₓ)dv(ₓ) = u(ₓ)v(ₓ)|ᵇ− ∫    v(x)du(x).
|
a

# П р и м е р 11.1.

Вычислим интеграл

π
∫ x cos x dx = ∫         x d sin x = x sin x|π− ∫  sin x dx =
|
0

π
= − ∫     sin x dx = cos x|π= −2.
|
0
```

---

## Query: "ряды сходимость признак Даламбера"

### #1 — score: 0.5982 🟡 Приемлемо

```
# П р и м е р 21.5.

Исследовать на сходимость ряд

∞

1 · 3 · 5 · . . . · (2n − 1)

Σ 3ⁿ(n + 1)!

n=1

Применим признак Даламбера:

lim aₙ₊₁ = lim 3n(n + 1)! · 1 · 3 · 5 · . . . · (2n + 1) =

n→∞ aₙ n→∞ 1 · 3 · 5 · . . . · (2n − 1) · 3n+1(n + 2)!

= lim 2n + 1 = 2 &#x3C; 1,

n→∞ 3(n + 2) 3

следовательно, данный ряд сходится.

# П р и м е р 21.6.

Исследовать на сходимость ряд

∞ √ ( n )³ⁿ

Σ n 2n − 1

n=1

Применим признак Коши в предельной форме:

√ √ ( n )³ⁿ √ ( n )³

lim n an = lim n n 2n −
```

### #2 — score: 0.5667 🟡 Приемлемо

```
# 3) Якобиан отображения

| *D(x; y)* | | | ϕu | ϕv | | |
| --------- | - | -- | -- | - |---|---|
| D(u; v)   | | | ψu | ψv | | |

Сохраняет постоянный знак в области G. Тогда справедливо равенство

∫                                                       |    (  ) |

∫                     ∫

f(x; y)dx dy =  ∫ f(ϕ(u; v); ψ(u; v)) |                D x; y      | du dv,

| D(u; v) |

|                       |

G                    D

(22.7)

Называемое формулой замены переменных в двойном интеграле.
```

### #3 — score: 0.5565 🟡 Приемлемо

```
# 8.6. Замечательные пределы

# Первый замечательный предел

Докажем, что имеет место равенство:

lim sin x = 1.
x→0  x

Исходя из геометрических рассмотрений (см. рисунок), имеем следующие неравенства для величин площадей:

| S OAC | < | S AC | < | S OAD |
| ----- | - | ---- | - | ----- |

Считая, что 0 &#x3C; x &#x3C; π, получим

| sin x | <     | x | < | tg x |
| ----- | ----- | - | - | ---- |
| 1     | <     | x | < | 1    |
| sin x | cos x | < | < | 1    |

Эти неравенства выполняются и для
```

---

## Query: "предел функции epsilon delta определение"

### #1 — score: 0.5992 🟡 Приемлемо

```
В нашем случае tg β = a(x + ∆x)² − ax² = 2ax + a∆x.

∆x                      ∆x → 0

Предельное положение секущей существует при                 ,

и тангенс угла наклона ее есть

tg α = y (x) = lim    y(x + ∆x) − y(x)         = 2ax.

∆x→0       ∆x

В общем случае, если у функции f существует  o e -

ная производная в точке x₀, то существует и касательная

к графику функции y = f(x) в точке x₀, и производная

равна тангенсу угла               α наклона касательной. Уравнение

касательной записыв
```

### #2 — score: 0.5632 🟡 Приемлемо

```
есть бесконечно малая функция при ∆x → 0. Отсюда

lim ∆f(∆x) = lim [f(x + ∆x) − f(x)] = 0,

∆x→0 ∆x→0

т. е. lim f(x + ∆x) = f(x), что означает непрерывность

∆x→0

функции f(x) в точке x.

Обратное неверно. Это иллюстрирует следующий p - мер.

# Пример 9.1.

Функция f(x) = |x| непрерывна в o - кe x = 0, но производная в точке x = 0 не существует, так как не существует предел lim |∆x|.

∆x→0 ∆x

Производная и арифметические операции связаны c e-дующими правилами.

# Теорема 9.2.

Пусть функции f
```

### #3 — score: 0.5562 🟡 Приемлемо

```
# 9.1. Определение и геометрический смысл производной функции в точке

Определение 9.1. Производной функции y = f(x) в точке x называется число

f (x) = lim f(x + ∆x) − f(x),

∆x→0 ∆x

если такой предел существует.

Задача о проведении касательной к графику функции y = f(x) в точке x тоже приводит к необходимости решить подобного рода предельный переход.

Рассмотрим, например, функцию y = ax² и ее график (рис. 9.1). Проведем касательную к этой кривой в точке M(x, ax²). Касательной к кривой в точ
```

---

## Query: "теорема Лагранжа о среднем значении доказательство"

### #1 — score: 0.6788 🟡 Приемлемо

```
# Теорема 8.7 (теорема Кантора)

Непрерывная на отрезке функция равномерно непрерывна на этом отрезке.

# Доказательство

Проведем методом от противного. Предположим, что f не является равномерно непрерывной на [a, b], т. е. существует ε₀ > 0 такое, что для любого δ > 0 найдутся x', x'' ∈ [a, b] такие, что |x' − x''| &#x3C; δ и |f(x') − f(x'')| ≥ ε₀.

Возьмем δ = δₙ → 0. Тогда найдутся две последовательности x', x'', разность между которыми стремится к нулю, а разность между последовательностями
```

### #2 — score: 0.6132 🟡 Приемлемо

```
# Принцип предельной точки

# Теорема 5.3 (теорема Больцано–Вейерштрасса для множеств)

Любое ограниченное бесконечное множество R имеет предельную точку.

# Доказательство

Пусть X ⊂ R. X – ограничено, т. е. найдутся два числа a и b такие, что

a &#x3C; x &#x3C; b, x ∈ X     ⇔     X ⊆ [a, b].

Разделим отрезок [a, b] пополам и рассмотрим две его половинки: [a, a+b] и [a+b, b]. Множество X – бесконечное, поэтому возможны два случая:

1. в каждом из отрезков [a, a+b], [a+b, b] лежит бесконечное ч
```

### #3 — score: 0.6125 🟡 Приемлемо

```
# Теорема 8.5.

Если функция f непрерывна на отрезке [a, b], ее значения на концах отрезка f(a) и f(b) не равны нулю и имеют разные знаки, то на интервале (a, b) есть по крайней мере одна точка c такая, что f(c) = 0.

# Доказательство.

Пусть f(a) &#x3C; 0 &#x3C; f(b). Обозначим отрезок [a, b] = ∆₀. Разделим его пополам. Если в середине отрезка ∆₀ функция равна нулю, то теорема доказана. Если нет, то обозначим за ∆₁ = [a₁, b₁] ту из половин отрезка [a, b], на концах которой функция f имеет разны
```

---

## Query: "непрерывность функции разрыв"

### #1 — score: 0.7108 🟢 Отлично

```
# 8. Непрерывность функции

# 8.1. Точки непрерывности и разрыва функции

Определение 8.1. Пусть функция f определена в окрестности точки x₀. Функция f называется непрерывной в точке x₀, если

lim f(x) = f(x₀).
x→x₀

Используя определение предела функции по Коши и Гейне, получим следующие развернутые определения непрерывности функции в точке.

Определение 8.2. Функция f называется непрерывной в точке x₀ по Коши, если

∀ ε > 0  ∃ δ(ε) > 0  ∀ x ∈ Oδ(x₀) ⇒ |f(x) − f(x₀)| &#x3C; ε.

Определение 8.3.
```

### #2 — score: 0.6737 🟡 Приемлемо

```
# 8.2. Функции, непрерывные на отрезке

Определение 8.6. Говорят, что функция f на отрезке [a, b], если она непрерывна во всех точках интервала (a, b), непрерывна справа в точке a, т. е.

lim f(x) = f(a),

x→ a+0

и непрерывна слева в точке b, т. е.

lim f(x) = f(b).

x→ b−0

# Теорема 8.3

(первая теорема Вейерштрасса). Если функция непрерывна на отрезке, то она ограничена на нем.

# Доказательство.

Пусть функция f непрерывна на [a, b]. Необходимо доказать, что

∃ M > 0 ∀ x ∈ [a, b] |f(x)| M.

```

### #3 — score: 0.6687 🟡 Приемлемо

```
Постоянная функция f(x) ≡ c интегрируема на любом отрезке [a, b], так как для любого разбиения T

| n           | n         | b                        |
| ----------- | --------- | ------------------------ |
| Σ f(ξₖ )Δₓₖ | = c Σ Δₓₖ | = c(b − a) = ∫ c dx = I. |
| k =1        | k =1      | a                        |

# Теорема 11.1 (необходимое условие интегрируемости).

Если f интегрируема на отрезке [a, b], то она ограничена на этом отрезке.

# Д о к а з а т е л ь с т в о.

Пусть функция f не 
```

---
