# RAG Quality Test: Matan Textbook

**Material ID:** `60c80b12-1f6a-4916-9112-63de4e403da1`  
**Total chunks indexed:** 339

---

## Query: "производная функции определение правило"

### #1 (score: -4.1694)

```
# 9.1. Определение и геометрический смысл производной функции в точке

Определение 9.1. Производной функции y = f(x) в точке x называется число

f (x) = lim f(x + ∆x) − f(x),

∆x→0 ∆x

если такой предел существует.

Задача о проведении касательной к графику функции y = f(x) в точке x тоже приводит к необходимости решить подобного рода предельный переход.

Рассмотрим, например, функцию y = ax² и ее график (рис. 9.1). Проведем касательную к этой кривой в точке M(x, ax²). Касательной к кривой в точ
```

### #2 (score: -5.0680)

```
# 15. Дифференцируемость функции многих переменных

# 15.1. Частные производные

Определение 15.1. Пусть функция f(x, y) определена в некоторой окрестности точки (x₀, y₀). Если существуют конечные пределы

lim f(x₀ + ∆x, y₀) − f(x₀, y₀)

∆x→0                                 ∆x

и

lim f(x₀, y₀ + ∆y) − f(x₀, y₀)

∆y→0                                 ∆y

то их называют частными производными функции f в точке (x₀, y₀) по переменным x и y соответственно и обозначают

∂f (x₀, y₀ ) или f' (x₀ , y₀ )  
```

### #3 (score: -5.4563)

```
Аналогично определяются дифференциалы более высоких порядков:

dⁿy = δ (d(ⁿ−1)y)|.

| x = x0

δx = dx

# 9.6. Производная функции, заданной параметрически

Пусть функции

{ x = x(t), (9.5)

y = y(t)

определены, непрерывны на некотором промежутке (α, β), а функция x(t) строго монотонна на нем. Тогда для функции x(t) существует обратная функция t = t(x), определенная на некотором промежутке (a, b) и, следовательно, имеет смысл сложная функция f(x) = y(t(x)). Эта функция y = f(x) называется функци
```

---

## Query: "интеграл Ньютона-Лейбница формула"

### #1 (score: -4.0231)

```
# 11.5. Интеграл с переменным верхним пределом. Формула Ньютона–Лейбница

Пусть функция f интегрируема на [a, b]. Функция

F(x) = ∫ax f(t)dt, x ∈ [a, b],

называется интегралом с переменным верхним пределом интегрирования.

# Теорема 11.8.

Пусть функция f интегрируема на отрезке [a, b]. Тогда интеграл с переменным верхним пределом интегрирования непрерывен на отрезке [a, b].

# Доказательство.

Из интегрируемости функции f следует ее ограниченность, т. е. существует M > 0 такое, что |f(t)| ≤ M 
```

### #2 (score: -4.1121)

```
области σ. Тогда имеет место следующая формула,  a  -

ваемая формулой Остроградского–Грина³² :

∫

∫ ( ∂Q − ∂P ) dσ =  P(x, y) dx + Q(x, y) dy,

∂x  ∂y

σ                   L

где двойной интеграл берется по области σ, а  p  o   e -

ный интеграл — вдоль замкнутого контура L, o pa    -

вающего область σ. При этом контур L проходится в  o-

ложительном направлении, т. е. при движении вдоль него

область σ остается слева (рис. 23.1).

Рис. 23.1

Применим формулу Остроградского–Грина к     c e-


```

### #3 (score: -4.3382)

```
# П р и м е р 23.2.

Вычислить криволинейный интеграл

∫ (ₓ₂ + y₂) dx + 2xy dy

AB

вдоль дуги кубической параболы y = x³ от точки A(1; 1) до точки B(2; 8).

# Р е ш е н и е.

Применяя формулу (23.4), получим

∫ (ₓ₂ + y₂) dx + 2xy dy =

AB

2

= ∫ ((x² + (x³)²) + 2x · x³ · 3x²) dx =

1

2

= ∫(x² + 7x⁶) dx = 129  1.

3

1

# Определение 23.2.

Если криволинейный интеграл

∫ Ф dr от вектор-функции

Ф = P(x, y, z )i + Q(x, y, z )j + R(x, y, z )k

берется по замкнутому контуру L, то он называется к
```

---

## Query: "ряды сходимость признак Даламбера"

### #1 (score: -3.0807)

```
# 21. Ряды

21.1. Основные определения

21.2. Признаки сходимости рядов с неотрицательными e- нами

21.3. Признаки сходимости знакопеременных рядов

21.4. Теоремы о группировке и перестановке рядов

21.5. Область сходимости функционального ряда. C e e - ной ряд. Радиус сходимости степенного ряда

21.6. Равномерная сходимость функциональной oc e o- вательности и функционального ряда

21.7. Примеры решения задач

# 22. Кратные интегралы

22.1. Двойные интегралы

22.2. Приложения двойных интегралов
```

### #2 (score: -3.7516)

```
# 21.6. Равномерная сходимость функциональной последовательности и функционального ряда

Радиус сходимости степенного ряда может быть числен по формуле Коши–Адамара31

R = limn→∞ √|an|

В частных случаях радиус может быть найден по формулам

R = limn→∞ |an| или R = limn→∞ |an+1|, если такие пределы существуют.

Доказательство этих формул в случае, когда c √|an| имеет верхний предел: limn→∞ |an|, следует из предельных знаков Коши и Даламбера.

# Определение 21.4

Функциональная последовательность
```

### #3 (score: -3.7677)

```
# Определение 5.8.

Последовательность отрезков {[aₙ, bₙ]} называется стягивающейся, если

∀ ε > 0 ∃ N(ε) ∈ N ∀ n > N(ε) ⇒ bₙ − aₙ &#x3C; ε.

(В этом случае говорят, что длины отрезков стремятся к нулю.)

# Лемма 5.2 (о вложенных стягивающихся отрезках).

Любая последовательность вложенных стягивающихся отрезков имеет общую точку, и притом только одну.

# Д о к а з а т е л ь с т в о.

Для любого n ∈ N имеем aₙ &#x3C; bₙ, так как левый конец отрезка меньше правого. Покажем, что

aₙ &#x3C; bₘ , n,
```

---

## Query: "предел функции бесконечность epsilon delta"

### #1 (score: -8.0786)

```
В нашем случае tg β = a(x + ∆x)² − ax² = 2ax + a∆x.

∆x                      ∆x → 0

Предельное положение секущей существует при                 ,

и тангенс угла наклона ее есть

tg α = y (x) = lim    y(x + ∆x) − y(x)         = 2ax.

∆x→0       ∆x

В общем случае, если у функции f существует  o e -

ная производная в точке x₀, то существует и касательная

к графику функции y = f(x) в точке x₀, и производная

равна тангенсу угла               α наклона касательной. Уравнение

касательной записыв
```

### #2 (score: -8.2865)

```
Д о к а з а т е л ь с т в о. В определении epe p e- мости положим ∆y = 0, а ∆x = 0 и получим

f(x₀ + ∆x, y₀) − f(x₀, y₀) = A∆x + o(∆x).

Поделим на ∆x и посчитаем предел

lim f(x₀ + ∆x, y₀) − f(x₀, y₀) = lim A + o(∆x) = A,

∆x→0 ∆x ∆x→0 ∆x

т. е. существует ∂f(x₀, y₀ ) и она равна A.

Учитывая доказанное свойство, условие epe - руемости функции можно записать в виде

∆f(x₀, y₀) = ∂f (x₀, y₀ )∆x+ ∂f (x₀, y₀ )∆y+o(√∆x² + ∆y²).

Часто бывает удобна следующая (эквивалентная) op- ма определения диффе
```

### #3 (score: -8.3734)

```
есть бесконечно малая функция при ∆x → 0. Отсюда

lim ∆f(∆x) = lim [f(x + ∆x) − f(x)] = 0,

∆x→0 ∆x→0

т. е. lim f(x + ∆x) = f(x), что означает непрерывность

∆x→0

функции f(x) в точке x.

Обратное неверно. Это иллюстрирует следующий p - мер.

# Пример 9.1.

Функция f(x) = |x| непрерывна в o - кe x = 0, но производная в точке x = 0 не существует, так как не существует предел lim |∆x|.

∆x→0 ∆x

Производная и арифметические операции связаны c e-дующими правилами.

# Теорема 9.2.

Пусть функции f
```

---

## Query: "теорема Лагранжа о среднем значении"

### #1 (score: -4.2115)

```
# Теорема 8.7 (теорема Кантора)

Непрерывная на отрезке функция равномерно непрерывна на этом отрезке.

# Доказательство

Проведем методом от противного. Предположим, что f не является равномерно непрерывной на [a, b], т. е. существует ε₀ > 0 такое, что для любого δ > 0 найдутся x', x'' ∈ [a, b] такие, что |x' − x''| &#x3C; δ и |f(x') − f(x'')| ≥ ε₀.

Возьмем δ = δₙ → 0. Тогда найдутся две последовательности x', x'', разность между которыми стремится к нулю, а разность между последовательностями
```

### #2 (score: -4.5844)

```
# Теорема 8.4 (вторая теорема Вейерштрасса)

Функция, непрерывная на отрезке, достигает на нем свои верхнюю и нижнюю грани, т. е. найдутся точки c₁, c₂ ∈ [a, b] такие, что

f(c₁) = inf f(x),

f(c₂) = sup f(x).

x∈[a,b]

# Доказательство

Докажем существование точки c₂. Пусть

M = sup f(x),

x∈[a,b]

1. ∀ x ∈ [a, b]  f(x) ≤ M,
2. ∀ ε > 0  ∃ xε ∈ [a, b]  f(xε) > M − ε.

Тогда

∀ n ∈ N  ∃ xₙ ∈ [a, b]  M − 1 &#x3C; f(xₙ) &#x3C; M.

n

Таким образом, построена ограниченная последовательность {xₙ}, из
```

### #3 (score: -4.6097)

```
# 21.6. Равномерная сходимость функциональной последовательности и функционального ряда

Радиус сходимости степенного ряда может быть числен по формуле Коши–Адамара31

R = limn→∞ √|an|

В частных случаях радиус может быть найден по формулам

R = limn→∞ |an| или R = limn→∞ |an+1|, если такие пределы существуют.

Доказательство этих формул в случае, когда c √|an| имеет верхний предел: limn→∞ |an|, следует из предельных знаков Коши и Даламбера.

# Определение 21.4

Функциональная последовательность
```

---

## Query: "матрица определитель вычисление"

### #1 (score: -4.2793)

```
# 11.7. Приложения определенного интеграла

# Вычисление площадей

Пусть функция y = f(x) определена, непрерывна и неотрицательна на отрезке [a, b]. Плоская фигура, ограниченная дугой графика функции на этом отрезке и прямыми x = a, x = b, y = 0, называется криволинейной трапецией (рис. 11.1).

Рис. 11.1

Площадь криволинейной трапеции определяется по формуле

S = ∫ab f(x)dx.

Площадь фигуры, ограниченной непрерывными на отрезке [a, b] кривыми y = f₁(x), y = f₂(x) и прямыми x = a, x = b, при усл
```

### #2 (score: -4.7558)

```
# 9.1. Определение и геометрический смысл производной функции в точке

Определение 9.1. Производной функции y = f(x) в точке x называется число

f (x) = lim f(x + ∆x) − f(x),

∆x→0 ∆x

если такой предел существует.

Задача о проведении касательной к графику функции y = f(x) в точке x тоже приводит к необходимости решить подобного рода предельный переход.

Рассмотрим, например, функцию y = ax² и ее график (рис. 9.1). Проведем касательную к этой кривой в точке M(x, ax²). Касательной к кривой в точ
```

### #3 (score: -4.7927)

```
затем подставляем найденные значения в функцию f и исследуем полученную функцию n − m переменных:

f(g₁(xₘ₊₁, . . . , xₙ), . . . , gₘ (xₘ₊₁, . . . , xₙ ), xₘ₊₁, . . . , xₙ ),

на обычный локальный экстремум.

Если точка (x⁰m+1, . . . , x⁰n) является точкой локального экстремума такой сложной функции, то точка (x⁰1, . . . , x⁰n), где

x⁰1 = g₁(x⁰m+1, . . . , x⁰n), . . . , x⁰m = gₘ (x⁰m+1, . . . , x⁰n),

является точкой условного экстремума функции f o  oc - тельно уравнений связи (18.3).

# П р и
```

---
